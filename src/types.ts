export type CommitmentStatus='unanswered'|'promised'|'in_progress'|'fulfilled'|'broken'|'disputed';
export type EvidenceItem={id:string;kind:'receipt'|'proof';title:string;sourceKind:'signed_document'|'media'|'press_link'|'document_link'|'link_only';sourceUrl:string;hasMedia?:boolean;mediaType?:string;quote?:string;direction?:'supports'|'refutes';verdict:'pending'|'verified'|'rejected'|'contested';documentDate:string;reviewedAt?:string};
export type TimelineEvent={id:string;date:string;title:string;detail:string;type:'promise'|'evidence'|'status'|'correction'};
export type Commitment={id:string;slug:string;title:string;detail:string;state:string;stateSlug:string;district:string;districtSlug:string;locality?:string;category:string;status:CommitmentStatus;promisedOn:string;deadlineStart?:string|null;deadline:string|null;deadlineLabel:string|null;progress:number;beneficiaries:string;accountableOffice:string;accountablePerson?:string;lastReviewedAt:string;evidence:EvidenceItem[];timeline:TimelineEvent[];pendingCompletionProofs?:number};
export type ExtractedDraft={title:string;promiseText:string;sourceUrl:string;promisedOn:string;deadlineStart:string;deadlineEnd:string;deadlineLabel:string;state:string;district:string;category:string;accountableOffice:string;confidence:Record<string,number>;warnings:string[]};
